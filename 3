#Initialize digital attendence system with zeroes

num_students = len(sd)
attendence_system = np.zeros(num_students,dtype = int)
print('Initialized digital attendence System(first 10 entries)')
print(attendence_system[:10])
